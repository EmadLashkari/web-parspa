"use client";

import React, { ReactNode, useState } from "react";
import { motion } from "framer-motion";

const OpinionAnime = ({ children }: { children: ReactNode }) => {
  return (
    <motion.div className="flex  flex-col justify-center items-center relative"></motion.div>
  );
};

export default OpinionAnime;
